import { data } from "../constants.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">NUESTRO EQUIPO</h1>
          <p>
            Nuestro talentoso equipo de cocina fusiona creatividad y dedicación,
            transformando ingredientes frescos en obras maestras gastronómicas
            que deleitan todos los sentidos.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
